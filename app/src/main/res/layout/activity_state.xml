<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".StateActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="New Button"
        android:id="@+id/SubmitButton"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="35dp" />

    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="New CheckBox"
        android:id="@+id/isTiredBox"
        android:layout_above="@+id/SubmitButton"
        android:layout_alignRight="@+id/SubmitButton"
        android:layout_alignEnd="@+id/SubmitButton"
        android:layout_marginBottom="28dp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/nameBox"
        android:layout_alignParentTop="true"
        android:layout_alignLeft="@+id/SubmitButton"
        android:layout_alignStart="@+id/SubmitButton"
        android:layout_marginTop="35dp"
        android:layout_alignRight="@+id/isTiredBox"
        android:layout_alignEnd="@+id/isTiredBox" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:ems="10"
        android:id="@+id/ageBox"
        android:layout_below="@+id/nameBox"
        android:layout_alignLeft="@+id/nameBox"
        android:layout_alignStart="@+id/nameBox"
        android:layout_marginTop="90dp"
        android:layout_alignRight="@+id/nameBox"
        android:layout_alignEnd="@+id/nameBox" />
</RelativeLayout>
